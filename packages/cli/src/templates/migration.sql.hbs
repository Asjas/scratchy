-- Migration: {{name}}
-- Generated: {{timestamp}}
--
-- Write your custom SQL migration below.
-- NOTE: Drizzle Kit auto-generates schema migrations via `pnpm drizzle-kit generate`.
-- Use this file for manual data migrations or custom SQL operations that
-- cannot be expressed in your Drizzle schema.
--
-- Example:
--   UPDATE "my_schema"."users" SET "role" = 'member' WHERE "role" IS NULL;
--   CREATE INDEX IF NOT EXISTS "users_role_idx" ON "my_schema"."users" ("role");
